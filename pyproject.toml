[project]
name = "touchstone-dqi"
version = "0.1.0"
description = "AI evaluation harness that seals results into an evidence bundle anyone can re-check offline."
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
authors = [{ name = "Quantile Labs" }]
maintainers = [{ name = "Quantile Labs" }]
keywords = ["evaluation", "ai-assurance", "audit", "reproducibility"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Quality Assurance",
    "Topic :: Software Development :: Testing",
]
dependencies = [
    "pydantic>=2.11",
    "pyyaml>=6.0",
    "typer>=0.16",
]

[project.urls]
Homepage = "https://github.com/Quantile-Labs/touchstone"
Source = "https://github.com/Quantile-Labs/touchstone"

[project.scripts]
touchstone = "touchstone.cli:app"

[dependency-groups]
dev = ["pytest>=8.0", "ruff>=0.12", "mypy>=1.15", "types-PyYAML>=6.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/touchstone"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
files = ["src/touchstone"]
python_version = "3.12"
strict = true

# The Protocol in backends/base.py is the reason this runs. runtime_checkable was
# measured: isinstance passes a backend whose signatures are wrong, and only a missing
# method or a missing isolation attribute fails it. mypy is what makes it a contract.
#
# `files` is a decision and not an oversight. src/ is checked because the package ships
# py.typed, so every annotation there is a promise to whoever imports it. tests/ is not,
# because a wrong fixture fails its own test on the next run, which is a faster and
# louder signal than a type error, and annotating thirty files of literals buys a second
# check on the one thing already covered. What this gives up is a test helper whose
# signature has drifted from the function it exercises. Revisit when a shared fixture
# module is large enough to have a contract of its own; tests/conftest.py is not yet.

[tool.pytest.ini_options]
testpaths = ["tests"]
